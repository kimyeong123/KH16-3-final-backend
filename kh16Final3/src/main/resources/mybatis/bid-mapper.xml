<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="bid">

    <select id="sequence" resultType="int">
        select bid_seq.nextval
        from dual
    </select>

    <insert id="add">
        insert into bid(
              bid_no
            , product_no
            , bidder_no
            , amount
        ) values (
              #{bidNo}
            , #{productNo}
            , #{bidderNo}
            , #{amount}
        )
    </insert>

 
    <select id="listByProduct" resultType="BidDto">
        select *
        from bid
        where product_no = #{productNo}
        order by bid_time desc
    </select>

</mapper>
